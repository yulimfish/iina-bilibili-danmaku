# IINA 哔哩哔哩弹幕映射插件 · 构建目标

日期：2026-09-03 · 状态：源码与安装包已实现，待 IINA 实机验证 · 仓库：`Yulimfish/iina-bilibili-danmaku`

## 一 · 一句话目标

做一个 IINA 插件：**在播放本地视频文件时，手动映射一个 B 站弹幕源（普通视频 BV 号 或 番剧剧集），把对应弹幕叠加到本地视频上**，弹幕随播放进度同步显示。

## 二 · 核心需求（两条弹幕源通道）

用户必须能用**任意一种**方式为当前本地视频指定弹幕源：

1. **普通视频（BV 号）通道**
   - 直接输入 BV 号（如 `BV1xx411c7mD`）
   - 或粘贴完整视频 URL（`https://www.bilibili.com/video/BVxxx`）
   - 自动解析出该视频（含**多 P / 分 P** 列表，允许选 P）
2. **B 站番剧（剧集）通道**
   - 搜索番剧名称 → 命中剧集（season）列表 → 选中后列出**分集（episode）列表** → 选某一集
   - 或直接粘贴番剧链接：`ep` 号（`https://www.bilibili.com/bangumi/play/ep330798`）/ `ss` 号（`/bangumi/play/ss33802`）/ `md` 号（`/bangumi/media/md28229233`）

选中弹幕源后，弹幕渲染到播放窗口并跟随时间轴。

## 三 · 技术方案（已核实的事实）

### 3.1 插件形态
- IINA 插件 = 一个目录（`.iinaplugin` 包）+ `Info.json` + JS 入口；发布打包为 `.iinaplgz`
- **两个入口**：
  - **global entry**（app 级）：启动时执行，负责创建菜单、侧边栏 UI、无视频时的交互
  - **main entry**（每播放器实例）：控制当前播放器，监听 mpv 事件
- API 全部挂在 `iina` 对象上，模块包括：`core` / `event` / `mpv` / `menu` / `ui` / `overlay` / `sidebar` / `fileSystem` / `utils` / `openInBrowser` / `console`
- 参考：https://docs.iina.io 、类型定义 https://github.com/iina/iina-plugin-definition

### 3.2 ⚠️ 关键事实：IINA 没有原生弹幕 API
- 已核实 `iina-plugin-definition/iina/index.d.ts`（develop 分支）**无 danmaku 模块**
- 弹幕渲染必须自建，走 `iina.overlay`（WKWebView 覆盖层）：
  - `overlay.loadFile(path)` / `overlay.simpleMode()` + `setContent()` / `setStyle()`
  - `overlay.show()` / `overlay.hide()` / `overlay.setOpacity(opacity)` / `overlay.setClickable(false)`
  - 插件脚本 ↔ overlay 页面通过 `overlay.postMessage()` 与页面内 `iina.onMessage()` 通信（overlay webview 是独立 JS 上下文，**不能直接访问 iina API**）
- **渲染引擎推荐**：复用 [CommentCoreLibrary](https://github.com/jabbany/CommentCoreLibrary)（xjbeta 的 iina-plugin-danmaku 验证过），或 [niconicomments](https://github.com/xpadev-net/niconicomments)（danmaku-cosmos 用过）。推荐 CCL。
- 侧边栏控制面板走 `iina.sidebar`（可塞自定义 HTML），用于输入框 / 搜索 / 分集列表 / 设置

### 3.3 B 站数据接口（均无需登录）
- 普通视频信息（BV → cid/分P）：
  `GET https://api.bilibili.com/x/web-interface/view?bvid=<BV>`
  → `data.cid`（P1 弹幕池）、`data.title`、`data.pages[]`（每项 `cid` / `part` / `page`，多 P 用）
- 番剧信息：
  - `GET https://api.bilibili.com/pgc/view/web/season?season_id=<ss>`（ss 通道）
  - `GET https://api.bilibili.com/pgc/view/web/season?ep_id=<ep>`（ep 通道）
  - `GET https://api.bilibili.com/pgc/review/user?media_id=<md>`（md → 取 ss，再走 season 接口）
  - 以上返回 `data.episodes[]`，每项含 `id`（ep 号）、`cid`（弹幕池）、`title` / `long_title`（集标题）、`badge`（会员标）
  - 搜索番剧：`GET https://api.bilibili.com/x/web-interface/search/type?search_type=media_bangumi&keyword=<关键词>`（返回 season_id 列表）
- 弹幕 XML（三条通道共用，`oid` = 对应 cid）：
  `GET https://api.bilibili.com/x/v1/dm/list.so?oid=<cid>`
  → 标准 B 站 XML：`<i><d p="进度秒,模式,字号,颜色,ctime,池,midHash,dmid">文本</d>...</i>`
- **风控要求（硬）**：所有请求必须带 `Referer: https://www.bilibili.com` 和正常 UA；pgc 系列接口不带 referer 会 412。请求可在 main entry 里用原生 `fetch` 发起（IINA 的 JS 环境支持 fetch），referer 通过自定义 header 传

### 3.4 弹幕 XML 解析要点
- `<d p="…">` 第一个字段 = 弹幕出现时间（**秒**，float）
- 模式 mode：`1`=滚动、`4`=底部、`5`=顶部、`6`=逆向滚动；`7`/`8`/`9`（高级/代码/BAS）**不支持，直接丢弃**
- 字号 size：映射到 18/25/36；颜色 color 为十进制 RGB
- 解析后转为渲染引擎需要的结构（CCL 的 `CommentData`：`mode` / `text` / `stime` / `size` / `color`）

## 四 · UI / 交互设计

主入口：菜单栏 `Plugins → Bili Danmaku` + 侧边栏面板。

侧边栏布局（`iina.sidebar`）：

```
┌─────────────────────────────┐
│ [标签A] 弹幕源    [标签B] 设置 │
├─────────────────────────────┤
│ ① 弹幕源                     │
│  [BV号/视频链接]  [搜剧集]    │  ← 输入方式切换
│  ┌───────────────────────┐  │
│  │ 输入框（BV号 或 URL）   │  │
│  └───────────────────────┘  │
│  [▶ 加载弹幕]  [分P/选集下拉] │
│  ├─────────────────────┤    │
│  │ 状态：已加载「标题」      │  │
│  │ · 分P1 / 分P2 …        │  │  ← 普通视频多P 或 番剧分集列表
│  └─────────────────────┘    │
├─────────────────────────────┤
│ ② 弹幕控制                   │
│  [x] 显示弹幕   [x] 置顶/底部  │
│  字号[────] 透明度[────]      │
│  速度[────]  偏移[+2s] ←→    │
│  清空弹幕 [重置偏移]          │
└─────────────────────────────┘
```

- 搜索剧集流程：`搜剧集` 页签 → 输入关键词 → 结果列表（剧集名+年份）→ 点击 → 分集列表 → 点击某集 → 加载
- 偏移调整用于本地文件与 B 站片源起止不一致时的对齐（已有插件均含此功能）
- 弹幕渲染在 `overlay`，`setClickable(false)` 保证不挡鼠标

## 五 · 验收标准（新 session 完成的定义）

1. 播放**本地视频**时，侧边栏输入 **BV 号**（或完整 URL）→ 弹幕加载并随进度同步显示
2. 对**多 P** 普通视频，能在分 P 列表中切换并加载对应弹幕
3. 搜索番剧名 → 出剧集 → 出**分集列表** → 选中某集加载弹幕 ✅；直接粘贴 **ep / ss / md** 链接同样可用
4. 滚动 / 顶部 / 底部三种基础弹幕正确渲染、时间轴对齐
5. 弹幕开关、字号、透明度、滚动速度、时间偏移 调节可用
6. 打包为 `.iinaplgz`，`IINA → 设置 → 插件 → 本地安装` 成功，重启后可用
7. 断网 / 无匹配（BV 无效、剧集地区受限）时给出明确错误提示，不崩溃

## 六 · 明确不做（Non-goals）

- B 站直播弹幕
- 在线播放 B 站视频/拉流（本插件只负责**映射弹幕到本地文件**）
- 发送/上传弹幕、登录账号
- 高级弹幕（mode 7）、代码弹幕（8）、BAS（9）
- 弹幕缓存与离线（可选加分项，不做硬性要求）

## 七 · 里程碑（建议实现顺序）

| 里程碑 | 内容 | 验收 |
|---|---|---|
| M1 骨架 | Info.json + 双入口 + 菜单 + 侧边栏面板 | 面板可开可关，不报错 |
| M2 BV 通道 | view API → 分P → cid → XML → 解析 → overlay 渲染 | 验收 1、2 |
| M3 番剧通道 | 搜索 + ep/ss/md 解析 + 分集列表 | 验收 3 |
| M4 控制 | 开关/字号/透明度/速度/偏移/清空 | 验收 5 |
| M5 打包与健壮性 | .iinaplgz + 错误提示 | 验收 6、7 |

## 八 · 已知坑（实现时注意）

- pgc API 不带 referer 必 412；先本地 curl 验证接口再写代码
- 番剧存在会员/地区限制，cid 可能取不到或取到后弹幕为空——错误信息要区分「视频不存在」「地区受限」「无弹幕」
- overlay 与插件脚本是**两个 JS 上下文**，只能 postMessage 通信，别在 overlay 里调 iina
- IINA 侧边栏对插件 UI 高度/滚动有限制，列表用内部滚动容器
- 弹幕时间戳是秒（float），渲染引擎按秒对时；本地文件时长与 B 站版本不一致时靠偏移解决

## 九 · 参考资料

- IINA 插件文档：https://docs.iina.io （Getting Started / Core / Overlay / Sidebar / Event）
- 类型定义：https://github.com/iina/iina-plugin-definition
- 参照实现：https://github.com/xjbeta/iina-plugin-danmaku （CCL 渲染）、https://github.com/karappo-yu/iina-plugin-danmaku-cosmos （双渲染/偏移/过滤）
- B 站参数与 API 说明：CSDN《B站参数及API介绍》（aid/ss/ep/cid 关系）
